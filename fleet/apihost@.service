[Unit]
Description=Api Host Service

After=nginx.service
After=redis.service
After=docker.service
After=elastic.service
After=rabbitmq.service
Before=apihost-discovery@%i.service
Requires=apihost-discovery@%i.service

[Service]
KillMode=none
Restart=always
TimeoutStartSec=0
EnvironmentFile=/etc/environment
ExecStartPre=-/usr/bin/docker rm -f apihost
ExecStartPre=/usr/bin/docker pull agiper/coreapp
ExecStart=/usr/bin/docker run -m 512M -p 81:81 -p 8081:8081 -v /home/core/volume/host/api:/var/lib/host -e EXEC='Rolene.Server.Host.Api.dll' -h apihost-%i --name apihost agiper/coreapp
ExecStop=/usr/bin/docker stop apihost

[X-Fleet]
Conflicts=apihost@*.service
