version: "3"

networks:
  rolene_development:
    external: true
  rolene_testing:
    external: true
  rolene_staging:
    external: true
  rolene_production:
    external: true

services:
  
  # **************** INFRASTRUCTURE *****************

  # MSSQL
  mssql:
    image: agiper/mssql
    networks:
      - rolene_development
    ports:
      - "1433"
    environment:
      - ENVIRONMENT=development
      - SA_PASSWORD=rX2Ja!293F8@
    deploy:
      resources:
        reservations:
          cpus: '0.25'
          memory: 512M
        limits:
          cpus: '0.75'
          memory: 2048M
      restart_policy:
        delay: 5s
        window: 60s
        condition: on-failure
      placement:
        constraints:
          - node.role == worker
          - node.labels.environment == development

  # REDIS
  redis:
    image: agiper/redis
    networks:
      - rolene_development
    ports:
      - "6379"
    deploy:
    environment:
      - ENVIRONMENT=development
      resources:
        reservations:
          cpus: '0.25'
          memory: 512M
        limits:
          cpus: '0.75'
          memory: 2048M
      restart_policy:
        delay: 5s
        window: 60s
        condition: on-failure
      placement:
        constraints:
          - node.role == worker
          - node.labels.environment == development

  # ELASTIC
  elastic:
    image: agiper/elastic
    networks:
      - rolene_development
    ports:
      - "5601"
      - "9200"
      - "9300"
    environment:
      - http.host=0.0.0.0
      - cluster.name=agiper
      - node.name=development
      - ENVIRONMENT=development
      - transport.host=127.0.0.1
      - bootstrap.memory_lock=true
      - ES_JAVA_OPTS=-Xms1g -Xmx1g
    deploy:
      mode: global
      resources:
        reservations:
          cpus: '0.25'
          memory: 1024M
        limits:
          cpus: '0.75'
          memory: 2048M
      restart_policy:
        delay: 5s
        window: 60s
        condition: on-failure
      placement:
        constraints:
          - node.role == worker
          - node.labels.environment == development

  # RABBITMQ
  rabbitmq:
    image: agiper/rabbitmq
    networks:
      - rolene_development
    ports:
      - "5672"
      - "15672"
    environment:
      - ENVIRONMENT=development
    deploy:
      resources:
        reservations:
          cpus: '0.25'
          memory: 512M
        limits:
          cpus: '0.75'
          memory: 2048M
      restart_policy:
        delay: 5s
        window: 60s
        condition: on-failure
      placement:
        constraints:
          - node.role == worker
          - node.labels.environment == development
  
  # ****************** SERVICES *******************

#WEBHOST
  webhost:
    image: localhost:5000/rolene_development/webhost
    networks:
      rolene_development:
        aliases:
          - rolene_development_webhost
    ports:
      - "8080"
    environment:
      - ENVIRONMENT=development
    deploy:
      mode: global
      resources:
        reservations:
          cpus: '0.25'
          memory: 256M
        limits:
          cpus: '0.75'
          memory: 1024M
      restart_policy:
        delay: 5s
        window: 60s
        condition: on-failure
      update_config:
        delay: 10s
        monitor: 60s
        parallelism: 1
        max_failure_ratio: 0.3
        failure_action: continue
      placement:
        constraints:
          - node.role == worker
          - node.labels.environment == development

  #APIHOST
  apihost:
    image: localhost:5000/rolene_development/apihost
    networks:
      rolene_development:
        aliases:
          - rolene_development_apihost
    ports:
      - "8081"
    environment:
      - ENVIRONMENT=development
    deploy:
      mode: global
      resources:
        reservations:
          cpus: '0.25'
          memory: 256M
        limits:
          cpus: '0.75'
          memory: 1024M
      restart_policy:
        delay: 5s
        window: 60s
        condition: on-failure
      update_config:
        delay: 10s
        monitor: 60s
        parallelism: 1
        max_failure_ratio: 0.3
        failure_action: continue
      placement:
        constraints:
          - node.role == worker
          - node.labels.environment == development
  
  #JOBHOST
  jobhost:
    image: localhost:5000/rolene_development/jobhost
    networks:
      rolene_development:
        aliases:
          - rolene_development_jobhost
    ports:
      - "8082"
    environment:
      - ENVIRONMENT=development
    deploy:
      mode: global
      resources:
        reservations:
          cpus: '0.25'
          memory: 256M
        limits:
          cpus: '0.75'
          memory: 1024M
      restart_policy:
        delay: 5s
        window: 60s
        condition: on-failure
      update_config:
        delay: 10s
        monitor: 60s
        parallelism: 1
        max_failure_ratio: 0.3
        failure_action: continue
      placement:
        constraints:
          - node.role == worker
          - node.labels.environment == development

  