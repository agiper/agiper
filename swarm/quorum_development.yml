version: "3.4"

networks:
  agiper_development:
    external: true

services:

  #WEB
  web:
    image: localhost:5000/agiper_development/quorum_web
    networks:
      agiper_development:
        aliases:
          - agiper_development_quorum_web
    ports:
      - "80"
    environment:
      - ENVIRONMENT=development
    deploy:
      replicas: 2
      mode: replicated
      restart_policy:
        delay: 1m
        window: 1m
        condition: any
      update_config:
        delay: 10s
        monitor: 60s
        parallelism: 1
        max_failure_ratio: 0.3
        failure_action: continue
      placement:
        constraints:
          - node.role == worker
          - node.labels.environment == development

  #API
  api:
    image: localhost:5000/agiper_development/quorum_api
    networks:
      agiper_development:
        aliases:
          - agiper_development_quorum_api
    ports:
      - "81"
    environment:
      - ENVIRONMENT=development
    deploy:
      replicas: 2
      mode: replicated    
      restart_policy:
        delay: 1m
        window: 1m
        condition: any
      update_config:
        delay: 10s
        monitor: 60s
        parallelism: 1
        max_failure_ratio: 0.3
        failure_action: continue
      placement:
        constraints:
          - node.role == worker
          - node.labels.environment == development

 #FUND
  fund:
    image: localhost:5000/agiper_development/quorum_fund
    networks:
      agiper_development:
        aliases:
          - agiper_development_quorum_fund
    environment:
      - ENVIRONMENT=development
    deploy:
      replicas: 2
      mode: replicated
      restart_policy:
        delay: 1m
        window: 1m
        condition: any
      update_config:
        delay: 10s
        monitor: 60s
        parallelism: 1
        max_failure_ratio: 0.3
        failure_action: continue
      placement:
        constraints:
          - node.role == worker
          - node.labels.environment == development

#ORDER
  order:
    image: localhost:5000/agiper_development/quorum_order
    networks:
      agiper_development:
        aliases:
          - agiper_development_quorum_order
    environment:
      - ENVIRONMENT=development
    deploy:
      replicas: 2
      mode: replicated
      restart_policy:
        delay: 1m
        window: 1m
        condition: any
      update_config:
        delay: 10s
        monitor: 60s
        parallelism: 1
        max_failure_ratio: 0.3
        failure_action: continue
      placement:
        constraints:
          - node.role == worker
          - node.labels.environment == development

  #MARKET
  market:
    image: localhost:5000/agiper_development/quorum_market
    networks:
      agiper_development:
        aliases:
          - agiper_development_quorum_market
    environment:
      - ENVIRONMENT=development
    deploy:
      replicas: 2
      mode: replicated
      restart_policy:
        delay: 1m
        window: 1m
        condition: any
      update_config:
        delay: 10s
        monitor: 60s
        parallelism: 1
        max_failure_ratio: 0.3
        failure_action: continue
      placement:
        constraints:
          - node.role == worker
          - node.labels.environment == development
